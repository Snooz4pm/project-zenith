/**
 * usePriceAlert hook - Stubbed for build compatibility
 * Price alert functionality (feature simplified)
 */
export function usePriceAlert() {
  return {
    createAlert: async (data: any) => ({ success: false }),
    alerts: [],
    loading: false,
    isCreating: false,
  };
}
