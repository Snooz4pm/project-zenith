/**
 * useIdleMode hook - Stubbed for build compatibility
 * Detects when the user is idle/inactive
 */
export function useIdleMode() {
  // Stubbed - always returns not idle
  return { isIdle: false };
}
